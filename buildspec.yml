version: 0.2

phases:
  install:
    commands:
      - echo "Installing Node.js"
      - aws --version
      - n 18
      - npm -v  
      - npm install -g aws-cdk
      - source .venv/bin/activate  
      - cdk --version  
  build:
    commands:
      - echo "Build commands go here"
